# Toy codes

Standalone teaching implementations: each file writes its algorithm's physics out by
hand instead of calling `tenet.network`, so you can read how the algorithm works.
For the lane rule and the file table, see the root [README](../../README.md#examples).

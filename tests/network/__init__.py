"""Package marker: ``tests/network/test_dmrg.py`` and ``tests/integration/test_dmrg.py``
share a basename, and pytest's default prepend import mode needs one of them to live in a
package to keep the two module names apart.
"""
